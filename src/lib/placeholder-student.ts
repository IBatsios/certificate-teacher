/**
 * PLACEHOLDER IDENTITY. Sign-in does not exist yet; it arrives in Task 02.
 * Until then every test attempt is recorded against this fixed key. It is a
 * constant in code, never a real account. Task 02 replaces every use of it
 * with the signed-in user's id and then deletes this file.
 */
export const PLACEHOLDER_STUDENT_KEY = "placeholder-student";
