<!-- Generated from docs/intake.md by kickoff v0.1.0. Edit the intake, not this file. -->

# teacher — Decisions

One line per decision. Newest at the bottom. Reasons come from the intake; where it gave none, the reason is "chosen in intake" and can be filled in later.

| # | Decision | Why | Source |
|---|---|---|---|
| D1 | Stack: TypeScript, Next.js, Next.js route handlers, PostgreSQL with Prisma, Tailwind, tests with Vitest and Playwright, pnpm | chosen in intake | Section 8 |
| D2 | Must use: Auth.js for email-and-password plus magic link; `@peculiar/x509` for parsing the certificates students upload | chosen in intake | Section 8 |
| D3 | Code lives on GitHub, public, licensed MIT | chosen in intake | Section 9 |
| D4 | Runs on Railway; environments: development, production | chosen in intake | Section 10 |
| D5 | Development database: Docker Compose | chosen in intake | Section 10 |
| D6 | Sign-in via email and password, magic link; roles: student, admin | chosen in intake | Sections 3, 6 |
| D7 | handoff_docs: true (differs from the default false) | chosen in intake | Section 9 |

---

## Added after the build

| # | Decision | Why | Source |
|---|---|---|---|
| D8 | Prisma pinned to 7.10.0 for the CLI, `@prisma/client`, and `@prisma/adapter-pg` | On 2026-09-06 the CLI's `latest` tag pointed at an 8.0 release candidate while the client's was 7.10.0; matched stable versions, with the v7 docs at prisma.io/docs/orm/v7 | Task 01 |
| D9 | Prisma 7 layout: `prisma7.config.ts` as `prisma init` writes it, client generated into `src/generated/prisma` (gitignored) by a `postinstall` script, Postgres reached through `@prisma/adapter-pg` | Prisma 7 requires a config file and a driver adapter; keeping the tool's own file name avoids fighting it | Task 01 |
| D10 | The Next.js app was scaffolded in a scratch folder and copied in | `create-next-app` refuses a directory holding `CLAUDE.md`, `.env`, `docker-compose.yml`, or a README | Task 01 |
| D11 | Vitest runs in Node only, for `src/**/*.test.ts`; no jsdom or Testing Library until a component test needs them. End-to-end tests are Playwright's, under `e2e/` | YAGNI; keeps CI free of a browser until Task 03 | Task 01 |
| D12 | CI runs lint and type check as well as the tests | Same three checks as the pre-commit hook, so a green pull request means the same thing locally and on GitHub | Task 01 |
| D13 | Skeleton pass rule: every question answered correctly. Placeholder until Task 06 sets the real pass mark | One question exists; any other rule would be invented | Task 01 |
| D14 | `.gitignore` excludes `backup-*.sql`, the files the runbook's backup step writes into the project root | A database dump must never be committed | Task 01 |
| D15 | Prettier and lint-staged skip `docs/` | `docs/intake.md` is hand-written and the rest is generated from it; tooling must not rewrite either | Task 01 |
| D16 | Three small additions the task file did not name: the home page's boilerplate replaced by one paragraph and a link to `/test`; Zod for the server action's form validation; the Next-generated `AGENTS.md` committed | The scaffold's demo page is not the product; the Projects-root conventions say Zod at every boundary; `next dev` re-creates `AGENTS.md`, so committing it keeps the tree clean | Task 01 review |
| D17 | Sessions are JSON web tokens in the cookie, with the Prisma adapter still in place for users and magic-link tokens | Auth.js throws `UnsupportedStrategy` when a Credentials provider runs with database sessions, and the magic link needs the adapter for its one-time token. The cookie's role is a snapshot; `src/lib/session.ts` re-reads the role from the database before pages and actions act. Amends the architecture doc's "sessions live in the database" | Task 02 |
| D18 | Magic-link email goes over SMTP through Auth.js's Nodemailer provider: Mailpit in development (`docker-compose.override.yml`, inbox at `http://localhost:8026`), any SMTP service in production, Resend's suggested. Variables `EMAIL_SERVER` and `EMAIL_FROM` replace `AUTH_RESEND_KEY` | Nothing needs an account to develop or test, the browser tests read the link from Mailpit, and switching providers later is an environment change only. Answers intake 7.1 | Task 02 |
| D19 | The first admin is whoever signs up with the address in `ADMIN_EMAIL`; everyone else starts as a student | No seed script and no first-user-wins rule; roles change afterwards on `/admin/users` | Task 02 |
| D20 | Visitors reach `/`, `/sign-in`, `/sign-up`, `/check-your-email`, and `/forbidden`; every other path needs a sign-in | The home page holds nothing sensitive and carries the sign-in link. Answers intake 6.4 | Task 02 |
| D21 | Role areas are exclusive: `/test` is for students, `/admin` for the admin; the admin uses a separate student account to walk the lessons | The PRD's role table lists what each role can do and the acceptance criterion says each role reaches exactly its allowed actions. Answers intake 6.3 | Task 02 |
| D22 | Role changes follow two rules: nobody changes their own role, and the last admin cannot be demoted. The decision is the pure `src/lib/role-change.ts` | Keeps someone able to manage roles at all times; pure so the rules have unit tests instead of database-dependent ones | Task 02 |
| D23 | Browser tests arrive in this task: Playwright under `e2e/`, the app on port 3100 with a per-run `ADMIN_EMAIL`, accounts ending in `@e2e.test` removed by a global teardown, and a second CI job with Postgres and Mailpit services | Sign-in working is the first line of the definition of done, and unit tests cannot prove it. Task 01 said the first slice with a screen installs Playwright; this is that slice | Task 02 |
| D24 | No rate limiting on sign-in yet | argon2 makes guessing slow and the audience is a handful of staff. Revisit in Task 08 with the platform's limiter, since a per-process counter would be wrong on a redeploy | Task 02 |
| D25 | `argon2` is allowed to run its build script in `pnpm-workspace.yaml` | pnpm 10 blocks install scripts by default; the package ships prebuilt binaries and the script only picks one | Task 02 |
| D26 | The route guard is `src/proxy.ts`, the Next 16 name for middleware, on the Node runtime. It decodes the cookie only, through `src/auth.config.ts`, which has no adapter; `src/auth.ts` adds the adapter and providers for pages and actions | Next's guidance: proxy checks are optimistic, the real check sits next to the data. The split keeps Prisma out of the proxy bundle | Task 02 |
| D27 | The Auth.js token type is not augmented; `src/auth.config.ts` validates the role found in the token at runtime with `isRole` | `next-auth/jwt` only re-exports `@auth/core/jwt`, which pnpm keeps out of root resolution, so an augmentation never merges. A runtime check also survives a bad or old cookie | Task 02 |
| D28 | The role change runs as one serializable transaction in `src/lib/users.ts`: read the target and the admin count, decide, write | Separate statements let two admins demote each other at the same moment and leave nobody in charge. Postgres aborts one of the two with a write conflict, shown as "could not be saved, try again" | Task 02 review |
| D29 | Whether an email has an account is not hidden: the sign-up page says so plainly, and sign-in with a password answers faster for an unknown email than for a wrong one | A clear message matters more to this audience than hiding it, and closing the timing channel alone would change nothing while the message stays. Revisit with rate limiting in Task 08 | Task 02 review |
| D30 | The admin account can only be created by a magic link; a password sign-up with the address in `ADMIN_EMAIL` is refused | Typing an address proves nothing, so a password sign-up could have claimed the admin role before the real admin did. A magic link proves the mailbox. Setting a password on the admin account afterwards is not built yet | Task 02 review |
| D31 | A role change applies after the person signs out and in again, and the forbidden page tells them so when it sees the cookie's role differ from the database's | The role in the cookie is only rewritten at sign-in (D17). Without the notice, a promoted person bounced between the forbidden page and their old home with no way out | Task 02 review |
| D32 | Ticking a step and starting over are server actions on the lesson page, not the two JSON routes the task named | Every form so far is a server action: it works without JavaScript and gets Next's origin check. Nothing in the app calls a JSON API. Routes can be added when something does | Task 03 |
| D33 | The app never deletes a learning session. "Start over" archives the active one, with its ticked steps, and starts an empty one; earlier sessions stay listed on the lesson page | Intake 5.2 and 5.3: progress is never lost, starting over is allowed | Task 03 |
| D34 | Lesson content is markdown under `content/lessons/<slug>`: `lesson.md` for the title and intro, and `NN-name.md` per step with a `key` and `title` header. Keys are permanent because progress is stored by key. The fence language names the shell: `shell` for both, `powershell`, `bash` for Mac or Linux, `text` for output. `react-markdown` renders it | Written by the developer, rendered, never stored; the file order is the lesson order | Task 03 |
| D35 | Commands are written for PowerShell on Windows and Terminal on Mac or Linux, with OpenSSL as the tool. On Windows the lesson installs OpenSSL with winget and falls back to the copy Git ships | The intake only says desktop. Every command was run on OpenSSL 3.5 while writing. The Windows install path has not been tried on a clean machine yet | Task 03 |
| D36 | Vitest now includes tests that hit the database: the session functions, tested as a caller sees them. `vitest.setup.ts` loads `.env`, CI's unit job has a Postgres service, and each test makes its own `@unit.test` user and removes it | The task asks for the data-access functions to be observed as a caller would, which no mock shows. Pure tests stay database-free | Task 03 |
| D37 | A student's home is the certificates lesson, and `/lessons` is a student-only area alongside `/test` | The lesson is the product; the test is its last step | Task 03 |
| D38 | A ticked step can be un-ticked | People mis-click, and it is the same code path in reverse | Task 03 |
| D39 | Sign-in is rate limited in memory: ten password attempts per email-and-address pair and thirty per address every fifteen minutes, five sign-in links per pair and twenty per address, ten sign-ups per address, and forty posts per address to the Auth.js routes. Each limiter remembers at most ten thousand keys. The address is Cloudflare's `cf-connecting-ip` | Revisits D24 now that sign-in faces the internet. Keying by pair means a stranger cannot lock a person out of their own account. One Railway instance makes a per-process counter correct; counts reset on a deploy. A shared store replaces it if a second instance ever appears | Task 08 review |
| D40 | Teacher lives at `teacher.ioannisbatsios.com`, not the apex | The apex already serves the personal website behind a Cloudflare Tunnel; the intake's "live at ioannisbatsios.com" predates that. The intake now names the subdomain | Task 08 |
| D41 | The Railway service runs with `pnpm start`, `pnpm prisma migrate deploy` as the pre-deploy command, the home page as the health check, and restart on failure; Node 22 is pinned in `.node-version` | Migrations run before traffic moves and a failed migration stops the deploy | Task 08 |
| D42 | Production email goes through Resend's SMTP endpoint with a verified sending subdomain such as `send.ioannisbatsios.com` | Same Nodemailer provider as development (D18), only the variables change. A sending subdomain keeps SPF and DKIM away from the personal domain's mail | Task 08 |
| D43 | Backups in production: a daily schedule on Railway's Postgres service, plus a manual backup before merging any migration, with a `pg_dump` through Docker as the terminal route | The runbook's rule, applied to the platform | Task 08 |
| D44 | Only Cloudflare can reach the app: a transform rule adds an `X-Origin-Secret` header to every request, and the proxy and the Auth.js routes refuse requests without it. The home page is exempt for Railway's health check | Railway's edge is public, so without this anyone could skip Cloudflare, forge the client address, and defeat the limits. The startup check in `src/instrumentation.ts` refuses to run with `AUTH_TRUST_HOST` but no `ORIGIN_SECRET` or `AUTH_URL` | Task 08 review |
| D45 | Every response carries HSTS, `nosniff`, `X-Frame-Options: DENY`, a referrer policy, and a permissions policy from `next.config.ts`; pages carry a Content-Security-Policy with a per-request nonce from `src/proxy.ts`. Styles allow inline attributes; scripts do not | The site is public with a login form. `poweredByHeader` is off | Task 08 review |
| D46 | The Prisma CLI is a runtime dependency, not a dev one | The pre-deploy command runs `prisma migrate deploy` in the built image; whether or not dev dependencies survive there must not decide if migrations run | Task 08 review |
| D47 | Nodemailer 9, outside Auth.js's declared peer range of 7 to 8 | Nodemailer 8 carries a high-severity advisory; 9.0.1 fixes it, and the magic-link journey passes on 9. Auth.js only declares the range it was tested with | Task 08 review |
| D48 | Railway's configuration lives in `.railway/railway.ts`, imported from the live project with `railway config pull`; `railway.json` is gone. Variables are `preserve()` so no value is in source. On Windows, `railway config plan` needs `$env:_` pointed at `railway.exe` before it runs | Railway retired `railway.json` for services created after mid-2026, so the first deploy ran with none of its settings, and the settings were applied to the service directly. The SDK's version check uses a Unix-only environment variable, which the Windows workaround supplies | Task 08 deploy |
| D49 | The proxy reads the session cookie with `getToken`, read-only, instead of Auth.js's `auth()` wrapper | The wrapper re-issued the cookie on every response, so a request sent with the old cookie that landed after sign-out, such as a link prefetch from the mouse crossing the header, brought the session back. Seen on the live site. Sessions now keep their sign-in expiry instead of rolling, which D17 already accepts | Task 08 live walk |
| D50 | Cloudflare features that rewrite HTML are off for `teacher.ioannisbatsios.com` through a configuration rule: Email Address Obfuscation and Rocket Loader | Obfuscation rewrote every email address in the page and injected a script; the content security policy blocked the script and React reported the rewritten HTML as a hydration error. The rule scopes the change to this hostname so the personal site keeps its settings | Task 08 live walk |

| D51 | The deploy lesson teaches nginx run through Docker Compose on port 8443, with `traefik/whoami` as the service behind it, not Caddy and not a native install | Task 04 has to pick a reverse proxy. A native nginx means three different installs, three config locations, and on Linux a service that fights for port 80; Docker makes one `docker compose up` correct on Windows, Mac, and Linux, and `docker compose down` removes it. `whoami` answers with the request as it received it, so the student reads `RemoteAddr`, plain `GET / HTTP/1.1`, and `X-Forwarded-Proto: https` and sees that TLS stopped at the proxy instead of being told so. The cost is Docker Desktop as a new prerequisite; 8443 avoids needing admin for a low port | Task 04 |
| D52 | The lesson page, the server actions, and the message and path helpers are shared: `src/lib/lesson-routes.ts`, `src/app/lessons/lesson-actions.ts`, and `src/app/lessons/lesson-view.tsx`. A lesson is a content folder, a slug, an eyebrow, and its completion note | The second lesson would otherwise have copied about 320 lines. `lesson.ts`, `lesson-progress.ts`, and `learning-session.ts` were already slug-agnostic, so only the page and the actions had to change. `lesson-actions.ts` is deliberately not a `"use server"` module, because such a module may only export async functions and so cannot export a factory; each lesson's `actions.ts` declares the real actions and passes its slug | Task 04 |
| D53 | Finishing the course means every step of every lesson in `COURSE_SLUGS`, not either lesson alone. `courseProgress` counts them, and step keys no lesson uses any more are ignored | Task 04 asks for session completion to span both lessons. Ignoring retired keys means removing a step from the content cannot leave an old session counted as more than finished. The per-lesson `lessonProgress` still drives each page's header, so the two questions stay separate | Task 04 |
| D54 | Certificates are read with Node's own `crypto.X509Certificate`, not `@peculiar/x509` as Task 05 suggested | It covers everything the task needs, verified against real lesson output: subject, issuer, dates, `subjectAltName`, the `ca` flag, `checkIssued`, and `verify` against the root's public key. The one path in the app that accepts a file from a stranger is the last place to add a dependency for no gain. Test fixtures live in `src/lib/__fixtures__` and were generated with OpenSSL 3.5.7: `openssl req -x509 -newkey rsa:2048 -nodes -keyout good-root.key -out good-root.crt -days 36500 -subj "/CN=My Root"`, then `openssl req -newkey rsa:2048 -nodes -keyout good-leaf.key -out good-leaf.csr -subj "/CN=localhost"` and `openssl x509 -req -in good-leaf.csr -CA good-root.crt -CAkey good-root.key -CAcreateserial -out good-leaf.crt -days 36500 -extfile leaf.ext`, with `-not_before 20200101000000Z -not_after 20210101000000Z` for the expired one and a second root for the foreign one. The good ones run to 2126 so no fixture expiry ever fails a build. No private keys are committed; the tests that cover refusing one build the text themselves | Task 05 |
| D55 | A submission holding a private key is refused on the raw text, before parsing, and refused again in `saveSubmission` before the write | Not defence in depth for its own sake. Node reads the first PEM block and ignores the rest, so a certificate with a key concatenated after it parses perfectly well; anything that inferred safety from a successful parse would store the key. What is stored is Node's own re-encoding of the certificate rather than the submitted text, so nothing sent around the PEM block is kept either | Task 05 |
| D56 | Checking a certificate is a server action, not the `src/app/api/certificates/route.ts` the task named | D32 again: every form in the app is a server action, it works without JavaScript, and nothing calls this programmatically. The proxy matcher covers it either way. The upload is capped at 16 KB per certificate in `parseCertificate`, oversized files are refused without being read, and `serverActions.bodySizeLimit` is 64 kb | Task 05 |
| D57 | Vitest runs one test file at a time (`fileParallelism: false`) | Two files now write to the one development database at serializable isolation. Run together they produced write conflicts and deleted each other's fixtures mid-test, neither of which said anything about the code. Each file also cleans up only its own users now, by email suffix. The suite takes about the same time either way | Task 05 |
